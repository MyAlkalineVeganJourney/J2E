# --- GENERAL ---

# Mac system file
.DS_Store

# Node modules and build files
node_modules/
build/
.cache/
coverage/
pnp.js
.pnp

# Environment configuration
*.env
.env*
/backend/.env
/backend/server/.env
/frontend/.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Media (Prevent pushing large videos)
*.mp4

# Ignore everything in the root except explicitly tracked files
*.log

# React & production keys (DO NOT put keys in .gitignore — manage via env vars instead)
# DO NOT put real keys in .gitignore
