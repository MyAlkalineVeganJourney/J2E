# --- MAC SYSTEM FILES ---
.DS_Store

# --- NODE MODULES & BUILD FILES ---
node_modules/
build/
.cache/
coverage/
pnp.js
.pnp

# --- ENVIRONMENT CONFIGURATION ---
*.env
.env*
/backend/.env
/backend/server/.env
/frontend/.env

# --- LOGS ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- MEDIA (Prevent pushing large videos) ---
*.mp4
*.mov
*.avi
*.mkv
*.webm

# --- BACKUP / UNWANTED FOLDERS ---
J2E-backup/
frontend-backup/
old-site/
.vscode/

# --- SYSTEM/OS FILES ---
Thumbs.db
ehthumbs.db
Desktop.ini

# --- REACT & BUILD-ONLY ---
/frontend/build/
/frontend/node_modules/

# --- OPTIONAL: GIT SECRETS ---
*.pem
*.crt
*.key
*.cert

# --- COMMENT ON DEPLOYMENT ---
# DO NOT IGNORE `.nojekyll` — it is needed for GitHub Pages + React
# DO NOT IGNORE `index.html` inside the React /build folder
